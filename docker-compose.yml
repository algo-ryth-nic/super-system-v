version: "3.7"

services:
  nodeserver:
    image: my-super-system
    hostname: localhost
    ports:
        - "3000:3000"
  mongodb:
    image: mongo:4.4.6
    # only for testing we expose this port
    # will remore this later
    ports:
      - 27017:27017